# Haruka Movie Streaming

MERN project layout:

- `frontend/` — React client application
- `backend/` — Express and MongoDB API
- `docs/` — system documentation and delivery milestones

Each application has its own dependencies and environment configuration.

## Documentation

- `docs/system/` contains architecture and technical documentation.
- `docs/milestones/` contains the project plan and milestone records.
